;;; packages.el --- web-beautify layer for Spacemacs -*- lexical-binding: t; -*-
;;
;; Compatibility layer for dotfile declarations like:
;;   web-beautify
;;
;; NOTE:
;; html/javascript/json/react layers may already integrate web-beautify. Keep
;; this layer empty to avoid init ownership conflicts during upgrades.

(defconst web-beautify-packages
  '()
  "Compatibility layer: no packages declared.")

;;; packages.el ends here
