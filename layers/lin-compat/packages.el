;;; packages.el --- lin-compat layer packages file for Spacemacs. -*- lexical-binding: t; -*-
;;
;; Compatibility layer to keep `init.el` minimal and upgrades stable.

(defconst lin-compat-packages
  '()
  "The list of Lisp packages required by the lin-compat layer.")

;;; packages.el ends here
