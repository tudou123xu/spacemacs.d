;;; packages.el --- lsp layer for Spacemacs -*- lexical-binding: t; -*-
;;
;; This is a compatibility layer to satisfy older dotfile declarations like:
;;   lsp
;;
;; Newer Spacemacs versions configure LSP via language layers directly.
;; Keeping this layer minimal avoids package init conflicts.

(defconst lsp-packages
  '()
  "Compatibility layer: no packages declared.")

;;; packages.el ends here
