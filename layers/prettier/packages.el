;;; packages.el --- prettier layer for Spacemacs -*- lexical-binding: t; -*-
;;
;; Compatibility layer for dotfile declarations like:
;;   prettier
;;
;; NOTE:
;; Formatting is typically owned by web/lang layers. Keep this layer empty to
;; avoid init ownership conflicts during upgrades.

(defconst prettier-packages
  '()
  "Compatibility layer: no packages declared.")

;;; packages.el ends here
